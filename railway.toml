[deploy]
startCommand = "python startup.py && uvicorn app:app --host 0.0.0.0 --port $PORT --workers 1 --timeout-keep-alive 30 --log-level info"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
