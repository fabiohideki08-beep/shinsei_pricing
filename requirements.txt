# Shinsei Pricing — dependências de produção
# Instale com: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
python-dotenv>=1.0.1
requests>=2.32.0
httpx>=0.27.0             # motor de frete — chamadas async (ViaCEP, Melhor Envio)
python-multipart>=0.0.9  # FastAPI form data / file uploads (UploadFile, File)

# ── Dados ─────────────────────────────────────────────────────
openpyxl>=3.1.2          # importação de regras via Excel

# ── Dev / Testes ──────────────────────────────────────────────
# Instale separado em CI: pip install pytest pytest-cov
# pytest>=8.0.0
# pytest-cov>=5.0.0
